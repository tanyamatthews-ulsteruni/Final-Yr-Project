describe('HomePage', () => {

	it('Home page should be created', () => {
		expect('HomePage').toBeTruthy();
	});

/*
  it('should show a title', () => {
    const h3: HTMLHeadingElement = fixture.debugElement.query(By.css('h3')).nativeElement;
    expect(h3.textContent).toContain('Welcome');
  });*/




});